# Create a Password-Secured Zip

## Overview
Macの基本機能でパスワード付きZipファイルを作成します。  
ターミナルからの操作になります。

### パスワード付きZipファイルの作成
``` bash
zip -e -r [Zipファイル化後の名前].zip [Zipファイル化するファイル名]
```

Enter password: という入力項目が出るので、こちらへ任意のパスワードを入力します。  
Verify password: という入力項目が出るので、こちらへ再度パスワードを入力します。
